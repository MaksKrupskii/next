name: Test

on:
  push:
    branches: [main]

jobs:
  build:
    if: ${{ github.ref == 'refs/head/main' }}
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo ${{ github.ref }}
